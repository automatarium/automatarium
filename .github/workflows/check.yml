name: CI tests
on: 
  push:
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install Yarn Dependencies
        run: yarn

      - name: Run Linter
        run: yarn lint

      - name: Run Tests
        run: yarn test
